
We start coding.
Because at that point, there are almost no surprises left.
You'll know:
every service
every database
every table
every endpoint
every gRPC call
every event
every responsibility


For that service we'll design:
The database tables.
Every column and its purpose.
Relationships.
Constraints and indexes.
Which fields are internal versus exposed.
The REST API.
The gRPC API.
The events it publishes and consumes.